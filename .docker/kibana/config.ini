KIBANA_STACK_VERSION=${KIBANA_STACK_VERSION}
KIBANA_PORT=${KIBANA_PORT}

SERVERNAME=kibana
ELASTICSEARCH_HOSTS=http://${PROJECT_NAME}-elasticsearch:${ELASTIC_PORT}
ELASTICSEARCH_USERNAME=${KIBANA_USERNAME}
ELASTICSEARCH_PASSWORD=${KIBANA_PASSWORD}